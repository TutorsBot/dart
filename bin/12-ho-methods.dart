
void main() {

  // map
  // filter
  // reduce
  // fold

  // collections - List, Map
  

  // Map Method
  // const number = [1, 2, 3, 4];
  // Map Definition

  // final square = number.map((number)=>number*number);
  // print(square);
  // print(square.toList());

  // Filter Method
  // final evens = square.where((element) => element.isEven);
  // print(evens);

  // const amounts = [ 199, 299, 299, 199, 499];
  // final total = amounts.reduce((value, element) => value + element);
  // print(total);

  // const amount = [199, 299, 299, 199, 499];
  // final totala = amount.fold(5, (int previousValue, element) => previousValue + element);
  // print(totala);

  // var desserts = ['cookies', 'pie', 'donuts', 'brownies'];
  // desserts.sort();
  // print(desserts);

  // var desserts = ['cookies', 'pie', 'donuts', 'brownies'];
  // desserts.reversed;
  // print(desserts);
  // print(desserts.reversed);

  // var dessertsReversed = desserts.reversed.toList();
  // print(dessertsReversed);

  var desserts = ['cake', 'pie', 'donuts', 'brownie'];
  // final bigDesserts = desserts.
  // where((dessert) => dessert.length > 3).
  // map((dessert) => dessert.toUpperCase())

  // final bigDesserts = desserts.
  // where((dessert) => dessert.length > 3).
  // map((dessert) => dessert.toUpperCase()).
  // reduce((value, element) => value+' '+element);
  
  // final bigDesserts = desserts.
  // where((dessert) => dessert.length > 3).
  // map((dessert) => dessert.toUpperCase()).
  // fold('SF',(String value, element) => value+' '+element);

  // print(bigDesserts);
}